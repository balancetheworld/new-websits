# Root directory - only ignore backend database files
# Web frontend has its own .gitignore in web/ folder

# Backend database
server/*.db
server/*.sqlite
server/*.sqlite3

# Ignore legacy frontend folders in root (use web/ instead)
.next
node_modules

